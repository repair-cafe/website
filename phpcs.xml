<?xml version="1.0"?>
<ruleset name="repaircafeStandard">
 <description>The repaircafe-specific coding standard.</description>

    <!-- Include the whole PSR-2 standard -->
    <rule ref="PSR2">
        <exclude name="Generic.Files.LineLength"/>
    </rule>

    <exclude-pattern>plugins/liip/*/updates</exclude-pattern>

</ruleset>
